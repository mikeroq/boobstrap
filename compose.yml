services:
  boobstrap:
    image: boobstrap:local
    build:
      context: .
      target: runtime
    init: true
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    security_opt:
      - no-new-privileges:true
